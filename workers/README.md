# Agentic-Workflow Workers

Background worker processes for Agentic-Workflow.

This package owns worker-side runtime settings, local workspace access, and
project-owned pacing/provider configuration. It does not own OpenClaw-native
Gateway secrets or browser-facing settings surfaces.
